# Copy to .env (or export directly) before running anova.py.
# See this repo's README.md > "Conectar la base de datos desde Railway" for
# how to get this value.
DATABASE_URL=postgresql://user:password@host:5432/railway
