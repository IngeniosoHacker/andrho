# Base URL of andrho-api (see ../andrho-api). Used by src/lib/api.js to POST
# the waiting-list survey to POST /waitlist. Vite inlines this at BUILD time,
# so it must be set wherever this app is built (Railway service variables),
# not just locally.
VITE_ANDRHO_API_URL=http://localhost:8080
